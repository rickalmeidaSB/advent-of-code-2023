/*
#include "DayN.h"
#include <iostream>
#include <string>
#include <sstream>

std::string DayN::GetName()
{
    return typeid(this).name();
}

void DayN::Run()
{
    std::istringstream stream(this->input);
    std::string line;

    // Iterate over each line
    while (std::getline(stream, line))
    {
        std::cout << line << std::endl;
    }
}
*/